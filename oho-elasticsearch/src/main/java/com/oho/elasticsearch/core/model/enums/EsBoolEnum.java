package com.oho.elasticsearch.core.model.enums;

/**
 * The enum Es bool enum.
 *
 * @author Sparkler
 * @createDate 2023 /1/9
 */
public enum EsBoolEnum {
    /**
     * Must es bool enum.
     */
    MUST,
    /**
     * Must not es bool enum.
     */
    MUST_NOT,
    /**
     * Should es bool enum.
     */
    SHOULD
}
